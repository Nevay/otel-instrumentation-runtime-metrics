file_format: '1.0'

log_level: warn

resource:
  detection/development:
    detectors:
      - composer:
      - container:
      - host:
      - process:

meter_provider:
  readers:
    - pull:
        exporter:
          prometheus/development:
            host: 0.0.0.0
            without_scope_info: true
